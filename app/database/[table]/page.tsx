import Shell from '@/components/Shell'
import {sql} from '@/lib/server'
import Link from 'next/link'
export const dynamic='force-dynamic'
function ident(v:string){if(!/^[A-Za-z_][A-Za-z0-9_]*$/.test(v))throw new Error('Invalid identifier');return '"'+v.replaceAll('"','""')+'"'}
export default async function TablePage({params}:{params:{table:string}}){const raw=decodeURIComponent(params.table);const [schema,name]=raw.includes('.')?raw.split('.',2):['public',raw];const table=ident(schema)+'.'+ident(name);const cols=await sql<any>(`select column_name,data_type,is_nullable,column_default from information_schema.columns where table_schema=$1 and table_name=$2 order by ordinal_position`,[schema,name]);let rows:any[]=[];let error='';try{rows=await sql(`select * from ${table} limit 100`)}catch(e:any){error=e.message}return <Shell><div className="flex gap-3 text-sm mb-5"><Link href="/database" className="muted hover:text-white">Database</Link><span className="muted">/</span><span>{schema}.{name}</span></div><h1 className="text-2xl font-semibold">{name}</h1><p className="muted text-sm mt-1">Up to 100 rows · {cols.length} columns</p><div className="card p-4 mt-6 overflow-auto"><h2 className="font-semibold mb-3">Schema</h2><table className="w-full text-xs"><thead><tr className="text-left muted border-b border-white/8"><th className="p-2">Column</th><th className="p-2">Type</th><th className="p-2">Nullable</th><th className="p-2">Default</th></tr></thead><tbody>{cols.map((c:any)=><tr key={c.column_name} className="border-b border-white/5"><td className="p-2">{c.column_name}</td><td className="p-2 muted">{c.data_type}</td><td className="p-2 muted">{c.is_nullable}</td><td className="p-2 muted max-w-xs truncate">{c.column_default||'—'}</td></tr>)}</tbody></table></div><div className="card p-4 mt-5 overflow-auto"><h2 className="font-semibold mb-3">Rows</h2>{error?<p className="text-red-300 text-sm">{error}</p>:rows.length===0?<p className="muted text-sm">No rows.</p>:<table className="min-w-full text-xs"><thead><tr className="text-left border-b border-white/8">{Object.keys(rows[0]).map(k=><th key={k} className="p-2 whitespace-nowrap">{k}</th>)}</tr></thead><tbody>{rows.map((r:any,i:number)=><tr key={i} className="border-b border-white/5">{Object.keys(rows[0]).map(k=><td key={k} className="p-2 align-top max-w-[280px] truncate" title={String(r[k]??'')}>{typeof r[k]==='object'?JSON.stringify(r[k]):String(r[k]??'')}</td>)}</tr>)}</tbody></table>}</div></Shell>}
