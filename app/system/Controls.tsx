'use client'
import {useState} from 'react'
export default function Controls({initial,environment}:{initial:any,environment:string}){const [enabled,setEnabled]=useState(!!initial?.maintenance_mode);const [busy,setBusy]=useState(false);const save=async()=>{setBusy(true);const r=await fetch('/api/maintenance',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({enabled:!enabled,environment})});const j=await r.json();setBusy(false);if(!r.ok)alert(j.error||'Failed');else setEnabled(j.enabled)};return <div className="card p-6 mt-7 max-w-3xl"><div className="flex items-center justify-between gap-5"><div><h2 className="font-semibold">Maintenance mode</h2><p className="muted text-sm mt-1">Uses the existing <code>site_settings</code> table. No developer table or role is created.</p></div><button onClick={save} disabled={busy} className={'w-14 h-8 rounded-full p-1 transition '+(enabled?'bg-amber-500':'bg-white/15')}><span className={'block w-6 h-6 rounded-full bg-white transition '+(enabled?'translate-x-6':'')}/></button></div><div className="mt-5 text-xs muted">Environment: <b className="text-white">{environment}</b> · Status: <b className={enabled?'text-amber-400':'text-emerald-400'}>{enabled?'maintenance':'live'}</b></div></div>}
